#include <nan.h>
#include "blake2.hh"

namespace B2JS {

using Nan::New;

using v8::Local;
using v8::Object;

void Init(Local<Object> exports) {
	exports->Set(New("blake2s").ToLocalChecked(), blake2s());
	exports->Set(New("blake2sp").ToLocalChecked(), blake2sp());
	exports->Set(New("blake2b").ToLocalChecked(), blake2b());
	exports->Set(New("blake2bp").ToLocalChecked(), blake2bp());
}

NODE_MODULE(addon, Init)

} //  B2JS
