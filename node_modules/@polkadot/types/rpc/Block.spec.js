"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _static = _interopRequireDefault(require("@polkadot/extrinsics/static"));

var _Method = _interopRequireDefault(require("../primitive/Method"));

var _Block = _interopRequireDefault(require("./Block"));

// Copyright 2017-2019 @polkadot/types authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
describe('Block', () => {
  beforeEach(() => {
    _Method.default.injectMethods(_static.default);
  });
  it('has a valid toRawType', () => {
    expect(new _Block.default().toRawType()).toEqual( // each of the containing structures have been stringified on their own
    JSON.stringify({
      header: JSON.stringify({
        parentHash: 'H256',
        number: 'Compact<u64>',
        stateRoot: 'H256',
        extrinsicsRoot: 'H256',
        digest: JSON.stringify({
          logs: 'Vec<{"_enum":{"Other":"Bytes","AuthoritiesChange":"Vec<AccountId>","ChangesTrieRoot":"H256","SealV0":"(u64,H512)","Consensus":"(u32,Bytes)","Seal":"(u32,H512)","PreRuntime":"(u32,Bytes)"}}>'
        })
      }),
      extrinsics: 'Vec<Extrinsic>'
    }));
  });
});