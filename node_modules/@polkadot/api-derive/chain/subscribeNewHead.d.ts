import { Observable } from 'rxjs';
import { ApiInterface$Rx } from '@polkadot/api/types';
import { AccountId, Header } from '@polkadot/types';
import { HeaderExtended } from '../type';
export declare type HeaderAndValidators = [Header, Array<AccountId>];
/**
 * @name subscribeNewHead
 * @returns An array containing the block header and the block author
 * @description An observable of the current block header and it's author
 * @example
 * <BR>
 *
 * ```javascript
 * api.derive.chain.subscribeNewHead(({ author, blockNumber }) => {
 *   console.log(`block #${blockNumber} was authored by ${author}`);
 * });
 * ```
 */
export declare function subscribeNewHead(api: ApiInterface$Rx): () => Observable<HeaderExtended>;
