/// <reference types="node" />
import { Server } from 'mock-socket';
import { Constructor } from '@polkadot/types/types';
export interface Global extends NodeJS.Global {
    WebSocket: Constructor<WebSocket>;
    fetch: GlobalFetch;
}
export declare type Mock = {
    body: Object;
    requests: Number;
    server: Server;
    done: () => {};
};
export declare type MockState$Subscription$Callback = (error: Error | null, value: any) => void;
export declare type MockState$Subscriptions = {
    [index: string]: {
        callbacks: {
            [index: number]: MockState$Subscription$Callback;
        };
        lastValue: any;
    };
};
export declare type MockState$Db = {
    [index: string]: Uint8Array;
};
export declare type MockState$Requests = {
    [index: string]: (db: MockState$Db, params: Array<any>) => string;
};
