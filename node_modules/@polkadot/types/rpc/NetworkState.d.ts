import StructAny from '../codec/StructAny';
/**
 * @name NetworkState
 * @description
 * Wraps the properties retrieved from the chain via the `system.network_state` RPC call.
 */
export default class NetworkState extends StructAny {
}
