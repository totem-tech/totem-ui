import { Observable } from 'rxjs';
/**
 * Shorthand for distinctUntilChanged(), publishReplay(1) and refCount().
 *
 * @ignore
 */
export declare const drr: () => <T>(source$: Observable<T>) => Observable<T>;
